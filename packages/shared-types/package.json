{
  "name": "@paymarh/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Types TypeScript partages entre l API PaymaRH et ses clients (back-office, et demain portail salarie / mobile).",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@paymarh/config": "workspace:*",
    "typescript": "~6.0.3"
  }
}
